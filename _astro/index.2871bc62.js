import{c as n,r as s,m as i}from"./render-template.9b130c00.js";import{u as l}from"./hoisted.be9d1f0e.js";const t=`<h2 id="css-list">CSS List</h2>
<p>In CSS, there are a lot of options for styling HTML lists. We can change its bullets, colors, positions etc.</p>
<p>If you’re unfamiliar with HTML Lists you can install this Learn HTML app for free!</p>
<p>Let’s have a quick review. In HTML there are two types of lists:
<code>&#x3C;ul></code> an unordered list; list items are marked with numbers or letters.</p>
<p>And then we have the <code>&#x3C;li></code> element which defines the list items for both unordered and ordered lists.</p>
<p>CSS List Bullet Styles/Types
The list-style-type CSS property sets the bullet type or marker to use.</p>
<p>Valid Values:
<code>none:</code> no bullet type
<code>disc:</code> a filled circle; default value.
<code>circle:</code> a hollow circle
<code>square:</code> a filled square
<code>decimal:</code> decimal numbers e.g. 1, 2, 3
<code>decimal-leading-zero:</code> decimal numbers with leading zeros e.g. 01, 02, 03
lower-roman: uppercase roman numerals e.g. I, II, III
<code>lower-alpha:</code> lowercase letters e.g. A, B, C
<code>lower-greek:</code> lowercase classical greek e.g.
<code>arabic-indic:</code> traditional Arabic-indic numbering
<code>georgian:</code> traditional Georgian numbering
<code>armenian:</code> traditional Armenian numbering</p>
<p>Here is an example of an unordered list bullet types</p>
<p>Here is an example of an ordered list bullet types.</p>
<p>CSS List Bullet Position
The <code>list-style-position</code> CSS property sets the position (whether it’s outside or inside) of the bullets.</p>
<p>Valid Values:
outside: sets the bullets to sit outside the list items ( default)
inside: sets the bullets to sit inside the list items.</p>
<p>CSS List Bullet Images.</p>
<p>In CSS, it’s possible to have inages as list bullets.</p>
<p>The list-style-image CSS property sets an image as bullets. Example:</p>
<p>Howerver, this property is very limited in terms of controlling the position, size, etc, of the bullets.</p>
<p>For that reason, we better use CSS background properties.</p>
<p>CSS List with Padding Margin and Colors
To make list look interesting we can add paddings, margins and colors.</p>
<p>Note! Any style to the <code>&#x3C;ul></code> and <code>&#x3C;ol></code>elements affects the entire list while any style added to the <code>&#x3C;li></code> element only affects list items.</p>
<p>[[CSS]]</p>`,o={title:"CSS List",description:"CSS List, CSS <ul>, CSS <li>, CSS List, CSS <ul>, CSS <li>.",publishDate:"21 Nov 2023",tags:["CSS"],minutesRead:"2 min read"},r="/home/ignacio/devhome/blog-dev-astro/src/content/post/css-list/index.md",a=void 0;function S(){return`
## CSS List

In CSS, there are a lot of options for styling HTML lists. We can change its bullets, colors, positions etc.

If you're unfamiliar with HTML Lists you can install this Learn HTML app for free!

Let's have a quick review. In HTML there are two types of lists:
\`<ul>\` an unordered list; list items are marked with numbers or letters.

And then we have the \`\`<li>\`\` element which defines the list items for both unordered and ordered lists.

CSS List Bullet Styles/Types
The list-style-type CSS property sets the bullet type or marker to use.

Valid Values:
\`none:\` no bullet type
\`disc:\` a filled circle; default value.
\`circle:\` a hollow circle
\`square:\` a filled square
\`decimal:\` decimal numbers e.g. 1, 2, 3
\`decimal-leading-zero:\` decimal numbers with leading zeros e.g. 01, 02, 03
lower-roman: uppercase roman numerals e.g. I, II, III
\`lower-alpha:\` lowercase letters e.g. A, B, C
\`lower-greek:\` lowercase classical greek e.g.
\`arabic-indic:\` traditional Arabic-indic numbering
\`georgian:\` traditional Georgian numbering
\`armenian:\` traditional Armenian numbering

Here is an example of an unordered list bullet types

Here is an example of an ordered list bullet types.

CSS List Bullet Position
The \`list-style-position\` CSS property sets the position (whether it's outside or inside) of the bullets.

Valid Values:
outside: sets the bullets to sit outside the list items ( default)
inside: sets the bullets to sit inside the list items.

CSS List Bullet Images.

In CSS, it's possible to have inages as list bullets.

The list-style-image CSS property sets an image as bullets. Example:

Howerver, this property is very limited in terms of controlling the position, size, etc, of the bullets.

For that reason, we better use CSS background properties.

CSS List with Padding Margin and Colors
To make list look interesting we can add paddings, margins and colors.

Note! Any style to the \`\`<ul>\`\` and \`<ol>\`elements affects the entire list while any style added to the \`\`<li>\`\` element only affects list items.

[[CSS]]
`}function g(){return t}function f(){return[{depth:2,slug:"css-list",text:"CSS List"}]}const y=n((d,c,p)=>{const{layout:u,...e}=o;return e.file=r,e.url=a,s`${i()}${l(t)}`});export{y as Content,g as compiledContent,y as default,r as file,o as frontmatter,f as getHeadings,S as rawContent,a as url};
